# Dependencies
node_modules/

# Web build output (built and deployed by .github/workflows/deploy.yml)
dist/
*.tsbuildinfo

# ESP-IDF firmware build output
firmware/build/
firmware/test/build/
sdkconfig
sdkconfig.old

# Component manager fetches these from the registry; dependencies.lock is
# committed so the pinned versions are reproducible.
firmware/managed_components/

# Editor / OS
.vscode/
.idea/
.DS_Store
*.swp
