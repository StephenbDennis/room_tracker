dependencies:
  # cJSON was a built-in `json` component until IDF v6.0 moved it to the registry.
  espressif/cjson: "^1.7.19"
  # ESP-MQTT moved out of IDF in v6.0 the same way. Headers are unchanged.
  espressif/mqtt: "^1.0.0"
